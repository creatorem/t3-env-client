const envClientConfig = {
  envsPath: './envs.ts', // path to the T3 envs file
  writePermission: true, // enable writing to env files
  envFilePath: '.env.local', // path where env vars will be written
};

export default envClientConfig;